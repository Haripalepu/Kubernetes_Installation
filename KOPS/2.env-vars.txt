# Replace <CLUSTER_NAME> with your desired cluster name
export CLUSTER_NAME=<CLUSTER_NAME>  # Example: k8sb26.xyz

# Replace <AWS_REGION> with your desired AWS region
export AWS_REGION=<AWS_REGION>  # Example: us-east-1

# Replace <NAME> with the desired name (can be same as CLUSTER_NAME)
export NAME=<NAME>  # Example: k8sb26.xyz

# Replace <KOPS_STATE_STORE> with your desired S3 bucket for state storage
export KOPS_STATE_STORE=<KOPS_STATE_STORE>  # Example: s3://k8s-2025

# Replace <KUBE_EDITOR> with your preferred text editor
export KUBE_EDITOR=<KUBE_EDITOR>  # Example: nano
